{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "process",
            "label": "g++ 编译",
            "command": "${workspaceFolder}\\build.bat",
            "args": [
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "通过 build.bat 调用 MSYS2 MinGW g++"
        }
    ]
}